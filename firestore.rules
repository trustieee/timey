rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read and write during testing
    match /playerProfiles/{userId} {
      allow read, write: if request.auth != null;
    }
  }
} 